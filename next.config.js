module.exports = {
  env: {
    PROJECT_ID: '2ERyKRezRxhHFmmpK4kq8a9WGaF',
    PROJECT_SECRET: '7cfbaa50aa1e7ee645ffd67215a6fa96',
    GOOGLE_KEY: 'AIzaSyBjlfjPTasTAjfvEwtdXUMuw9MgZ87krDY'
  },
  compiler: {
    // see https://styled-components.com/docs/tooling#babel-plugin for more info on the options.
    styledComponents: true
    /* | {
      // Enabled by default in development, disabled in production to reduce file size,
      // setting this will override the default for all environments.
      displayName?: boolean,
      // Enabled by default.
      ssr?: boolean,
      // Enabled by default.
      fileName?: boolean,
      // Empty by default.
      topLevelImportPaths?: [],
      // Defaults to ["index"].
      meaninglessFileNames?: [],
      // Enabled by default.
      cssProp?: boolean,
      // Empty by default.
      namespace?: string,
      // Not supported yet.
      minify?: boolean,
      // Not supported yet.
      transpileTemplateLiterals?: boolean,
      // Not supported yet.
      pure?: boolean,
    }, */
  }
}
